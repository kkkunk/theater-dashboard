# 演析 · 演出宣发转化看板 — 产品需求文档

> 版本：v0.2 | 日期：2026-08-08 | 作者：产品 & Codex

### v0.2 本次变更
- 下线复购率：当前数据无法稳定获取，本期不展示、不计算
- 新增预计售票数及售票完成率，用于跟踪售票目标进度
- 下线观众结构：本期不展示圈层、新老客、地域、年龄等分析
- 新增经营总表：按日/周/月汇总总售票数、总宣传量、会员增长数

---

## 一、产品定位

### 1.1 一句话
**演析**是一套面向演出行业宣发运营团队的数据看板，核心回答一个问题：**一场演出从宣发启动到演出售罄，新媒体声量如何驱动票房转化？**

### 1.2 产品目标
- 帮助宣发运营人员看清每场演出的**新媒体声量 → 票房转化**全链路
- 让老板/管理层一眼了解近期演出的**整体票房健康度**
- 通过历史数据复盘，辅助后续演出的**宣发预算分配和渠道选择**

### 1.3 产品边界
**做**：
- 票房与新媒体声量的对齐对比（趋势图、散点图、渠道拆解）
- 预计售票数与实际售票进度跟踪
- 按日/周/月汇总总售票数、总宣传量、会员增长数
- 单场演出下钻分析（宣发节奏 vs 票房曲线）
- 策略ROI概览（人工录入的影响金额/花费）
- LLM驱动的智能问数（自然语言 → SQL → 图表）

**不做**：
- 自动爬取/对接新媒体平台实时数据（考虑合规与实现成本）
- 自动归因推算策略贡献（策略效果依赖人工录入）
- 排片排期的预测引擎（后续可扩展）
- 复购率分析（当前无法稳定获取可计算复购率的数据）
- 观众结构分析（圈层、新老客、地域、年龄等本期不纳入）

---

## 二、目标用户与使用场景

### 2.1 用户画像

| 角色 | 核心场景 | 关注指标 |
|---|---|---|
| **宣发运营**（主要用户） | 每场演出宣发启动后，每日跟踪宣传量、售票进度和票房增长是否匹配；演出结束后复盘渠道效率和策略ROI | 宣传量趋势、售票完成率、渠道转化占比、首日% vs 中期%、策略ROI |
| **剧院管理层**（次要用户） | 打开看板扫一眼近期演出整体情况，发现异常数据后再让运营深入分析 | 总售票数、总票房、上座率、会员增长数、异常预警 |

### 2.2 使用频率
- **每日**：运营打开看板，查看正在进行中的演出数据、新媒体的新增声量
- **演出结束后**：运营做项目复盘，导出关键数据
- **每周/每月**：管理层看整体趋势

---

## 三、功能架构

### 3.1 功能全景

```
演析·演出宣发转化看板
├── 模块1：总览仪表盘（首页）
│   ├── 全局票房与声量对比趋势
│   ├── 经营总表（总售票数/总宣传量/会员增长数）
│   ├── 近期演出快速浏览卡片
│   ├── 渠道贡献占比
│   └── 异常预警提示
│
├── 模块2：单场演出分析（核心页面）
│   ├── 演出基本档案
│   ├── 声量→票房 双轴对比图
│   ├── 渠道转化拆解
│   ├── 预计售票完成进度
│   ├── 策略ROI复盘
│   └── 时段分析（首日/早鸟/压哨）
│
├── 模块3：渠道 & 策略复盘
│   ├── 渠道效率排名
│   ├── 策略类型效果对比
│   └── 历史最佳实践库
│
├── 模块4：智能问数（AI Agent）
│   ├── 自然语言查询
│   ├── 自动图表生成
│   └── 高频问题沉淀
│
└── 模块5：数据管理（后台）
    ├── 演出数据录入/导入（CSV）
    ├── 策略数据人工录入
    └── 外部信息批量导入
```

---

## 四、指标体系设计

### 4.1 核心指标（面向首页 & 管理层）

| 指标 | 定义 | 计算方式 | 数据来源 |
|---|---|---|---|
| **总票房（元）** | 所选时间范围内所有演出订单的票面金额总和 | SUM(票面总金额) | 订单表 |
| **总售票数（张）** | 所选时间范围内已完成销售的票数 | SUM(总售票张数) | 订单表 |
| **上座率** | 实售票数 / 总可售票数 | SUM(总售票张数) / SUM(发售票数) | 票价表 + 订单表 |
| **预计售票数（张）** | 单场演出预先设定的售票目标 | SUM(项目预计售票数) | 项目台账（人工录入/导入） |
| **售票完成率** | 实际售票数相对预计售票数的完成进度 | 总售票数 / 预计售票数 × 100% | 项目台账 + 订单表 |
| **总宣传量** | 所选时间范围内各宣传渠道可获得的宣传量合计 | SUM(各渠道宣传量) | 项目台账 + 外部信息表 |
| **会员增长数** | 所选时间范围内新增会员数量 | SUM(新增会员数) | 会员增长日表（导入） |
| **宣发转化效率** | 声量增长 vs 订单增长的对比关系 | 订单增长率 / 声量增长率 | 项目台账 + 订单表 |

### 4.2 运营指标（面向单场演出分析）

| 指标 | 定义 | 计算方式 |
|---|---|---|
| **首日票房占比** | 开售首日票房 / 总票房 | 首日售票张数 / 总售票张数 |
| **首周票房占比** | 开售首周票房 / 总票房 | 首周零售票房 / 预计零售票房 |
| **压哨票房占比** | 尾周+尾日票房 / 总票房 | (尾周+尾日) / 总票房 |
| **渠道贡献Top 5** | 各渠道订单数排名 | 按渠道分组 ORDER BY |
| **策略ROI** | 影响金额 / 策略花费 | 宣传策略表·影响金额 / 策略花费 |

### 4.3 指标口径规范

- **统计周期**：默认"最近30天"，支持按演出场次/按自然月切换
- **日票房**：以 **演出日期（场次日）** 为主口径，同时保留**下单日期**视角作为辅助
- **上座率**：分子=实售票数（总售票张数），分母=可售票数（发售票数）
- **预计售票数**：以项目/场次为单位人工录入；未录入时售票完成率显示“暂无目标”，不以0代替
- **售票完成率**：分子=实际售票数，分母=预计售票数；结果可超过100%
- **经营总表时间口径**：支持按日/周/月聚合；总售票数按下单日期、总宣传量按宣传发生日期、会员增长数按入会日期统计
- **会员数据边界**：本期只统计新增会员数量，不展示会员画像、会员复购或人群结构

---

## 五、页面详细设计

### 5.1 总览仪表盘（首页）

**页面目标**：让宣发运营一眼看到"今天整体票房怎么样、哪些演出声量在涨、有没有异常"。

**布局（从上到下）**：

1. **顶部指标卡片行**（4张卡片，深色磨砂玻璃背景）
   - 总票房（大字 + 环比涨跌箭头 + 迷你趋势线）
   - 上座率（仪表盘样式 + 同期对比）
   - 售票完成率（实际售票数 / 预计售票数 + 进度条）
   - 总宣传量（大字 + 环比涨跌 + 迷你趋势线）

2. **中部双区域（左右 6:4 分栏）**
   - 左：**票房 & 声量双轴趋势图**（折线图双Y轴，横轴=日期，左Y轴=日票房，右Y轴=声量指标，可切小红书/抖音/公众号）
   - 右：**渠道贡献饼图** + **单场售票完成率排行**（上下排列）

3. **经营总表（时间维度）**
   - 时间粒度：日 / 周 / 月切换
   - 表格字段：时间、总售票数、总宣传量、会员增长数
   - 各列展示当期值；支持按时间正序/倒序查看

4. **底部演出列表**（卡片式，可横向滚动）
   - 每张卡片显示：演出名称、海报占位、当前票房进度条、宣发声量等级（高/中/低）、策略标签
   - 票房进度条统一调整为：实际售票数 / 预计售票数
   - 点击卡片 → 跳转单场演出分析页

5. **异常预警条**（固定在列表上方）
   - 条件触发：宣传量高但售票完成率低、票房突降、上座率低于阈值等

### 5.2 单场演出分析（核心页面）

**页面目标**：这是看板的核心，把所有"声量→票房"的数据对齐到一个页面里，让运营做完整的项目复盘。

**布局**：

1. **演出档案头部**
   - 演出名称、剧团、导演、主演、场地、演出时间
   - 标签行：IP类型、题材、风格、情绪
   - 豆瓣评分、IP评分、宣发声量等级
   - 预计售票数、实际售票数、售票完成率

2. **声量→票房 对比时间线**（页面核心大图，占上半屏）
   - 横轴 = 演出时间线（从宣发启动到演出日）
   - 上方区域 = 宣发事件标注（策略开始/截止时间、公众号推文日、小红书爆款日）
   - 下方曲线 = 日票房累计曲线 + 声量柱状图叠加
   - 交互：鼠标悬停显示具体数值，点击宣发事件跳转详情

3. **渠道转化拆解**（中左）
   - 渠道订单排名条形图（公众号/app/小程序/网站/抖音/窗口/大麦/猫眼/秀动/票星球/小票点/主办）
   - 每个渠道标注销售额 & 占比

4. **售票目标完成进度**（中右）
   - 预计售票数、实际售票数、剩余目标数
   - 售票完成率进度环/进度条
   - 超额完成时展示超额比例，不截断为100%

5. **时段分析卡片行**（下半屏）
   - 首日/早鸟/正常/压哨 订单占比
   - 各时段的渠道偏好差异

6. **策略ROI复盘表**（底部）
   - 表格：策略名称、策略类型、花费、影响金额、ROI、效果标签
   - 支持展开详情

### 5.3 渠道 & 策略复盘

**页面目标**：跨演出的横向对比，帮助运营积累"哪种渠道/策略对哪类演出最有效"的认知。

**布局**：

1. **渠道效率排行榜**
   - 表格：渠道名称、累计销售额、订单数、占比、参与演出数、场均销售额
   - 过滤器：按演出类型、按时间范围

2. **策略类型效果对比**
   - 气泡图：横轴=花费，纵轴=影响金额，气泡大小=参与演出数，颜色=策略类型

3. **历史最佳实践库**
   - 卡片式列表：展示ROI最高的几场演出的宣发组合（渠道+策略+声量特点）

### 5.4 智能问数（AI Agent）

**页面目标**：让不熟悉SQL的运营人员通过自然语言查询数据。

**交互形态**：右下角浮动按钮 → 点击展开侧边对话面板

**能力范围（V1）**：
- 自然语言 → SQL 查询 → 返回表格/图表
- 示例问法：
  - "上周哪个演出票房最高"
  - "最近30天小红书声量前3的演出是哪些，它们的上座率怎么样"
  - "抖音渠道贡献最高的5场演出"
  - "哪些演出的售票完成率低于80%"
  - "本月总售票数、总宣传量和会员增长数分别是多少"

**技术方案**：
- 方案A（推荐）：LLM API（豆包/DeepSeek → 也可以直接用Codex内置能力）接收自然语言 → 提取意图+生成SQL → 查询本地SQLite → 返回结果 + 自动渲染图表
- 后端预处理：数据库 schema 作为 system prompt 的一部分，让LLM理解表结构

---

## 六、数据架构

### 6.1 数据库设计（SQLite 映射）

基于本期可获得的数据，SQLite 建表时保持字段名中文/拼音均可，我建议用拼音简写保证兼容性（后续可配中文别名）。

**六张核心表**：
1. `project_ledger` — 项目顶层台账（演出档案 + 宣发数据 + 预计售票数）
2. `promotion_strategy` — 宣传运营策略表
3. `ticket_price` — 票价表（场次 × 票档）
4. `order_detail` — 订单表（交易事实）
5. `external_info` — 外部信息表（宣传素材与宣传量）
6. `member_growth_daily` — 会员增长日表（日期 + 新增会员数）

### 6.2 表关联关系

```
project_ledger (project_id)
  ├── ticket_price (project_id + show_time)
  ├── promotion_strategy (project_id)
  ├── order_detail (project_id + show_time)
  └── external_info (project_id，按标题关键词匹配)

member_growth_daily (stat_date)
  └── 按日期与订单、宣传数据聚合到经营总表
```

### 6.3 模拟数据策略

- **演出数量**：8-12 场（覆盖不同品类：音乐剧、话剧、舞剧、儿童剧、音乐会）
- **时间跨度**：最近 90 天
- **数据特征**：
  - 植入真实的声量→票房转化模式（声量高峰后 3-5 天出现订单高峰）
  - 设置 2-3 场"异常案例"（声量高但上座率低、策略ROI为负等），用于展示预警功能
  - 渠道分布符合行业常识（大麦/猫眼占大头、公众号/抖音在增长）
  - 每场演出设置预计售票数，用于展示售票完成进度
  - 按日生成会员增长数据，用于经营总表的日/周/月聚合

---

## 七、视觉设计方向

### 7.1 整体风格
- **基调**：深色科技感 data-v 风格（深蓝/藏青底色 + 渐变光效卡片）
- **配色**：
  - 主色：#1A1A2E（深蓝黑底）、#00D4AA（青绿色强调）
  - 辅色：#4A90D9（数据蓝）、#F5A623（警示橙）、#E74C3C（异常红）
  - 卡片：#16213E（磨砂玻璃卡片底）
- **动效**：
  - 页面加载：卡片依次浮入
  - 数据切换：数字滚动效果
  - 图表：ECharts 标准交互动效（hover 高亮、tooltip 弹出）

### 7.2 排版原则
- 数据密度适中：首页核心卡片≤6张，单场分析页不超过一屏半
- 表格支持固定表头、列排序
- 移动端不考虑（比赛 Demo 以桌面端展示为主）

---

## 八、技术方案

### 8.1 技术栈

| 层 | 选型 | 说明 |
|---|---|---|
| 前端框架 | React 18 + TypeScript | 稳定性好，生态完善 |
| 构建工具 | Vite | 开发体验快 |
| UI 样式 | Tailwind CSS | 快速定制深色风格 |
| 图表库 | ECharts 5 + echarts-for-react | 数据看板标配，交互丰富 |
| 动效 | Framer Motion | 页面过渡 & 数字滚动 |
| 后端 | Node.js + Express | 轻量、统一语言 |
| 数据库 | SQLite (better-sqlite3) | 零配置，单文件，适合 Demo |
| AI Agent | 对接 LLM API（方案A） | 自然语言 → SQL |

### 8.2 项目结构

```
theater-dashboard/
├── client/                    # 前端 React 应用
│   ├── src/
│   │   ├── components/        # 通用组件（卡片、图表、表格）
│   │   ├── pages/             # 页面（首页、单场分析、渠道复盘）
│   │   ├── hooks/             # 自定义 hooks（数据获取、筛选状态）
│   │   ├── utils/             # 工具函数（数字格式化、颜色映射）
│   │   ├── styles/            # 全局样式 & Tailwind 配置
│   │   └── App.tsx
│   └── package.json
├── server/                    # 后端 Express 应用
│   ├── src/
│   │   ├── routes/            # API 路由（dashboard, show, channel, agent）
│   │   ├── models/            # 数据查询层（SQL 封装）
│   │   ├── db/                # SQLite 初始化 + 种子数据
│   │   └── agent/             # LLM Agent 模块
│   └── package.json
├── data/                      # 种子数据 + 导入模板 CSV
└── README.md
```

---

## 九、开发分期

### Phase 1：框架 + 数据层（预计 2-3 天）
- 项目脚手架搭建（前后端）
- SQLite 建表 + 种子数据生成
- 后端 API 基础接口（演出列表、票房汇总、渠道汇总）

### Phase 2：看板页面（预计 2-3 天）
- 总览仪表盘（首页）
- 单场演出分析页
- 渠道 & 策略复盘页

### Phase 3：智能问数 + 数据管理（预计 1-2 天）
- LLM Agent 接入
- 数据导入/编辑后台

### Phase 4：打磨（预计 1 天）
- 动效调优、异常预警、响应式微调

---

## 十、待确认事项

- [ ] PRD 整体方向是否认可？
- [ ] 页面结构（三页看板 + AI问数 + 后台管理）是否接受？
- [ ] 智能问数 Agent 的技术方案（对接 LLM API）确认？
- [ ] 是否有需要补充的分析场景或指标？
- [ ] 种子数据中是否需要植入特定"故事线"（例如某场音乐剧小红书爆了但票房没跟上）？
- [ ] 预计售票数的录入粒度：按项目还是按具体场次？（当前PRD按项目/场次兼容设计）
- [ ] 总宣传量的原始数据来源和统一统计单位是什么？
- [ ] 会员增长数据是否可以按日提供？（当前PRD按日导入，再聚合到周/月）

---

> **PRD 状态：待评审**  
> 以上内容基于我们此前的多轮讨论整理。请逐项确认或提出修改意见，对齐后进入开发阶段。
